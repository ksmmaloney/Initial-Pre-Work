//  The Math.random() function (capital "M") will generate  a random decimal number between 0 and 1. You need to create a "result" variable that will store a decimal generated by the Math.random function.

function randomFraction() {

  // Only change code below this line.

   var result = 0;
    while (result == 0) {
    console.log(result = Math.random());
  }
    return result;

  // Only change code above this line.
}
