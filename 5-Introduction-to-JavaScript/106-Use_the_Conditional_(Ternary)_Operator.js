/*  The conditional, or ternary, operator condenses an if-else expression into one line.

condition ? statement-if-true : statement-if-false;

*/

function checkEqual(a, b) {

    return a == b ? true : false;
}

checkEqual(1, 2);
