// You can check for multiple conditions by linking more than one conditional operator together.



unction checkSign(num) {

  return num > 0 ? "positive" : num < 0 ? "negative" : "zero" ;

}

console.log(checkSign(0));
