//  With arrow functions you can still pass one or more arguments into them, just like typical functions.

use strict";
const myConcat = (arr1, arr2) =>  arr1.concat(arr2);


// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
